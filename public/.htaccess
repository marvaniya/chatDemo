<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /test/

    # અસ્તિત્વમાં હોય તો direct serve
    RewriteCond %{REQUEST_FILENAME} -f [OR]
    RewriteCond %{REQUEST_FILENAME} -d
    RewriteRule ^ - [L]

    # બાકી બધું SPA index.html પર
    RewriteRule ^ index.html [L]
</IfModule>
